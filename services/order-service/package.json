{
  "name": "@ecobazar/order-service",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "check": "find src test -name '*.js' -exec node --check {} \\;",
    "test": "npm run check && node --test test/*.test.js"
  },
  "dependencies": {
    "@ecobazar/contracts": "^1.0.0",
    "@ecobazar/platform": "^1.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  }
}
